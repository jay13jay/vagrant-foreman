---
# vars file for common
admin_user: admin
admin_pass: test

norm_user: jay
norm_pass: test
userdirs:
  - /root/
  - /home/{{ admin_user }}
  - /home/{{ norm_user }}

hostname: foreman.local

common_packages:
  - vim
  - nmap
  - python-pip
  - git
  - wget
  - mlocate
  - which
  - expect

pip_packages:
  - passlib
  - pexpect
